현대적이고, 빠르며, 파이썬 표준 타입 힌트에 기초한 Python API를 빌드

⇒ Python에서 REST API 서버/웹 애플리케이션을 빠르게 개발할 수 있는 프레임워크

= Java의 Spring Boot

특징

• 빠름

• 빠른 코드 자성

• 적은 버그

• 직관적

• 쉬움

• 짧음

• 견고함

• 표준 기반

• ;;;

설치

pip install fastapi

pip install "uvicorn[standard]"

예제

만들기

• main.py 파일 만들기:

```python
from typing import Union
# Union[X, Y] : X 또는 Y를 의미
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
	return {"Hello": "World"}
	
@app.get("/items/{item_id}")
def read_item(item_id: int, q: Union[str, None] = None):
	return {"item_id": item_id, "q":q}
	
# 혹은 async def 사용
@app.get("/items/{item_id}")
async def read_item(item_id: int, q: Union[str, None] = None):
	return {"item_id": item_id, "q":q}
	
```

실행하기

- 서버 실행

- 브라우저로 http://127.0.0.1:8000/items/5?q=somequey 열기

심화

• PUT 요청에 있는 본문 받기