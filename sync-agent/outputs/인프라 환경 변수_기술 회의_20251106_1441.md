Domain 

```plain text
IP: 43.203.181.233
Domian: j13a104.p.ssafy.io

HTTPS 환경: https://j13a104.p.ssafy.io
HTTP  환경: http://j13a104.p.ssafy.io
```

Jenkins

```plain text
AccessToken: a1h-8j4ucQztFpwW_ELX
AdminPassword: 921f1b16f4c04c759b87224d994d2a99

# AdminPassword 확인 명령어
sudo cat /var/lib/jenkins/secrets/initialAdminPassword

# sudo /etc/~/jenkins.servicd.d/override.conf 파일로 재정의
[Service]
Environment="JENKINS_OPTS=--prefix=/jenkins"

# Jenkins 초기 설정
계정명: dokdokhae
암호: dokdokhae
이름: 김용범
이메일 주소: dt10002@naver.com
API Token: 11406619793d91edda63da8529fbe6ccbe

# WebHook 설정
- Push Event
- 다음 주소로 URL 설정 
- https://<JENKINS_USER_ID>:<API_TOKEN>@<DOMAIN>/jenkins/project/<JOB_NAME>

```

Nginx Setting 

HTTPS Setting 

```java
certbot 설정 이후 

// 443 포트 개방
ufw allow 443 

```

```yaml
services:
  gateway:
    image: bumnote/gateway:latest
    container_name: gateway
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      - be-biz
      - be-ai

  fe:
    image: bumnote/fe:latest
    container_name: fe
    restart: unless-stopped
    ports:
      - "5173:80"

  be-biz:
    image: bumnote/be-biz:latest
    container_name: be-biz
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
    expose:
      - "8080"

  be-ai:
    image: bumnote/be-ai:latest
    container_name: be-ai
    environment:
      - APP_ENV=prod
    restart: unless-stopped
    expose:
      - "8000"

networks:
  default:
    name: app-network
```

→ APP_ENV → ENV로 수정